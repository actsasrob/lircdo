Sanity check incoming requests can reach lircdo service using APP_FQDN and APP_PORT variables.
Test install/configure script

Ability to hardcode application pin so that test app can start with known PIN number to use for automated tests

think the line below in server.js should be using the session secret not a harcoded string???
app.use(session({secret: "Your secret key", cookie: { secure: true }}));

Clean-up messages returned by callback functions. More succinct message on success.

Merge nodejsv8 branch into master.
Remember to change git branch back to master in scripts/lirdo_install.sh

Re-deploy test/prod lircdo server instances from master branch
